//Verifica si un número es divisible por 2 o por 3. 

let num = prompt("Ingresa un numero.")

if (num % 2 === 0) {
    console.log("El número es divisible por 2");  //Se cumplio correctamente la operacion del bucle por lo que se muestra este resultado.
}else {(num % 3 === 0)
    console.log("El número es divisible por 3");  //Se cumplio correctamente la operacion del bucle por lo que se muestra este resultado.
}